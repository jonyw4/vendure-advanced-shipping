{
  "name": "@vendure-advanced-shipping/rodonaves",
  "description": "A plugin using the Advanced Shipping for calculate shipping using Rodonaves as provider",
  "version": "0.0.2",
  "license": "MIT",
  "main": "index.js",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "watch": "tsc -p ./tsconfig.build.json --watch",
    "build": "rimraf lib && tsc -p ./tsconfig.build.json",
    "lint": "eslint ./src --ext .ts,.tsx",
    "lint:fix": "eslint ./src --ext .ts,.tsx --fix"
  },
  "dependencies": {
    "rodonaves-js": "1.0.4"
  },
  "peerDependencies": {
    "@vendure-advanced-shipping/core": "^0.0.1",
    "@vendure/core": "^0.13.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
