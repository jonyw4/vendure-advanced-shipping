{
  "name": "@vendure-advanced-shipping/melhor-envio",
  "description": "A plugin using the Advanced Shipping for calculate shipping using Melhor Envio as provider",
  "version": "0.0.1",
  "license": "MIT",
  "main": "index.js",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "watch": "tsc -p ./tsconfig.build.json --watch",
    "build": "rimraf lib && tsc -p ./tsconfig.build.json",
    "lint": "eslint ./src --ext .ts,.tsx",
    "lint:fix": "eslint ./src --ext .ts,.tsx --fix"
  },
  "peerDependencies": {
    "@vendure/core": "^0.13.0",
    "@vendure-advanced-shipping/core": "^0.0.1"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "menv-js": "1.3.3"
  }
}
