{
  "name": "@vendure-advanced-shipping/melhor-envio",
  "description": "A plugin using the Advanced Shipping for calculate shipping using Melhor Envio as provider",
  "version": "0.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "watch": "tsc -p ./tsconfig.build.json --watch",
    "build": "rimraf lib && tsc -p ./tsconfig.build.json",
    "lint": "eslint --ext .ts,.tsx",
    "lint:fix": "eslint --ext .ts,.tsx --fix"
  },
  "dependencies": {
    "menv-js": "2.0.1"
  },
  "devDependencies": {
    "@vendure-advanced-shipping/core": "1.0.11",
    "@vendure/core": "0.13.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
