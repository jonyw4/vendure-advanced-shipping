{
  "name": "@vendure-advanced-shipping/ups-brazil",
  "description": "A plugin using the Advanced Shipping for calculate shipping using UPS Brazil as provider",
  "version": "1.0.6",
  "license": "MIT",
  "main": "lib/index.js",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "watch": "tsc -p ./tsconfig.build.json --watch",
    "build": "rimraf lib && tsc -p ./tsconfig.build.json",
    "lint": "eslint ./src --ext .ts,.tsx",
    "lint:fix": "eslint ./src --ext .ts,.tsx --fix"
  },
  "peerDependencies": {
    "@vendure-advanced-shipping/core": "^0.0.1",
    "@vendure/core": "^0.13.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "ups-brazil-js": "2.0.0"
  }
}
